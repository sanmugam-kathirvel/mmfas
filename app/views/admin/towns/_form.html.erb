<%= form_for(@admin_town) do |f| %>
  <% if @admin_town.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_town.errors.count, "error") %> prohibited this admin_town from being saved:</h2>

      <ul>
      <% @admin_town.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :town %><br />
    <%= f.text_field :town %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
